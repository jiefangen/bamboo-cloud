spring:
  application:
    name: payment-service
  profiles:
    active: junit
server:
  port: 11103
  servlet:
    context-path: /payment-service
    encoding:
      charset: UTF-8
      force: true
      enabled: true

logging:
  level:
    org.panda.service.doc: debug
