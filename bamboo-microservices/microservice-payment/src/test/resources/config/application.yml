spring:
  application:
    name: ms-payment
  profiles:
    active: junit
server:
  port: 11103
  servlet:
    context-path: /ms-payment
    encoding:
      charset: UTF-8
      force: true
      enabled: true

logging:
  level:
    org.panda.service.doc: debug
